#include <stdio.h>

/**
 * main - code causing infinite loop
 * Return: Return 0
 */

int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i = 0;

	/**
	 *While (i < 10)
	 *{
	 *	putchar(i);
	 *}
	 */

	printf("Infinite loop avoide!b \\o/\n");
	return (0);
}
